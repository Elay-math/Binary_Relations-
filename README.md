# Binary_Relations-
Two implementations of a binary relation property checker: one in C++ operating on the relation matrix, and another in Python that models the relation as a graph and analyzes its adjacency matrix. The program verifies whether the relation is reflexive, irreflexive, symmetric, and transitive.
